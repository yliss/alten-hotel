spring:
  profiles:
    active: dev
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    platform: postgres
    url: jdbc:postgresql://127.0.0.1:5432/postgres
    username: postgres
    password: test
    driverClassName: org.postgresql.Driver
server:
  port: 8081
  servlet:
    contextPath: /altenhotel/api/v1

